import React from 'react';
import './App.css';

type PropsType = {
    counter: number
    minValue: number
    maxValue: number

}

function Counter(props: PropsType) {
    return (
        <div className="counter">

        </div>
    );
}

export default Counter;
